apiVersion: v1
kind: Service
metadata:
  name: todo-backend-svc # Este nombre es el que usa el Frontend en BACKEND_URL
  namespace: project
spec:
  type: ClusterIP
  selector:
    app: todo-backend
  ports:
    - port: 80        # Puerto de red interna
      targetPort: 3001  # Puerto de la app Node