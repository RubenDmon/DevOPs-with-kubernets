name: Release Application

on:
  push:
    # Se ejecuta en cualquier rama para soportar entornos dinámicos

env:
  PROJECT_ID: ${{ secrets.GKE_PROJECT }}
  GKE_CLUSTER: dwk-cluster      # Nombre de tu clúster
  GKE_ZONE: us-east1-b          # Tu zona
  repo_name: repo-todo          # Nombre de tu repo en Artifact Registry
  region: us-east1              # Región del Artifact Registry

jobs:
  build-publish-deploy:
    name: Build, Publish and Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 1. Autenticación con Google Cloud
      - uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GKE_SA_KEY }}'

      # 2. Configurar Cloud SDK
      - name: 'Set up Cloud SDK'
        uses: google-github-actions/setup-gcloud@v2

      # 3. Configurar Docker
      - run: gcloud auth configure-docker ${{ env.region }}-docker.pkg.dev --quiet

      # 4. Obtener credenciales del Clúster GKE
      - name: 'Get GKE credentials'
        uses: 'google-github-actions/get-gke-credentials@v2'
        with:
          cluster_name: '${{ env.GKE_CLUSTER }}'
          project_id: '${{ env.PROJECT_ID }}'
          location: '${{ env.GKE_ZONE }}'

      # 5. Construir y Subir BACKEND
      - name: Build and Push Backend
        run: |
          IMAGE_TAG=${{ env.region }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.repo_name }}/todo-backend:${{ github.sha }}
          docker build -t $IMAGE_TAG ./project/todo-app/todo_backend
          docker push $IMAGE_TAG
          echo "BACKEND_IMAGE=$IMAGE_TAG" >> $GITHUB_ENV

      # 6. Construir y Subir FRONTEND
      - name: Build and Push Frontend
        run: |
          IMAGE_TAG=${{ env.region }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.repo_name }}/todo-frontend:${{ github.sha }}
          docker build -t $IMAGE_TAG ./project/todo-app/todo_frontend
          docker push $IMAGE_TAG
          echo "FRONTEND_IMAGE=$IMAGE_TAG" >> $GITHUB_ENV

      # 7. Configurar Kustomize
      - name: Set up Kustomize
        uses: imranismail/setup-kustomize@v2.1.0

      # 8. Desplegar (Aquí está toda la magia de los secretos)
      - name: Deploy
        env:
          # Secreto para la Base de Datos
          PG_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          # Secreto para el Backup (JSON de Google Cloud)
          BACKUP_KEY: ${{ secrets.GCP_BACKUP_KEY_JSON }}
        run: |
          # A. Determinar el nombre del namespace según la rama
          if [ "${{ github.ref_name }}" = "main" ]; then
            NAMESPACE="project"
          else
            NAMESPACE="${{ github.ref_name }}"
          fi
          
          echo "🚀 Desplegando en Namespace: $NAMESPACE"

          # B. Crear el namespace si no existe
          kubectl create namespace $NAMESPACE || true

          # C. Configurar el contexto
          kubectl config set-context --current --namespace=$NAMESPACE

          # --- AUTOMATIZACIÓN DE SECRETOS ---

          # 1. Crear secreto de contraseña Postgres
          kubectl create secret generic postgres-password \
            --from-literal=POSTGRES_PASSWORD="$PG_PASSWORD" \
            -n $NAMESPACE \
            --dry-run=client -o yaml | kubectl apply -f -

          # 2. Crear secreto de la llave Google Cloud (NUEVO)
          # Creamos el archivo temporalmente y generamos el secreto
          echo "$BACKUP_KEY" > key.json
          kubectl create secret generic google-cloud-key \
            --from-file=key.json=key.json \
            -n $NAMESPACE \
            --dry-run=client -o yaml | kubectl apply -f -
          rm key.json # Borramos el archivo temporal por seguridad

          # ----------------------------------

          # D. Entrar a la carpeta de Kustomize
          cd project/todo-app

          # E. Inyectar el namespace correcto
          kustomize edit set namespace $NAMESPACE

          # F. Inyectar las imágenes nuevas
          kustomize edit set image todo-backend-image=${{ env.BACKEND_IMAGE }}
          kustomize edit set image todo-frontend-image=${{ env.FRONTEND_IMAGE }}

          # G. Aplicar cambios al clúster
          kustomize build . | kubectl apply -f -

          # H. Verificar estado
          kubectl rollout status deployment/todo-backend-dep -n $NAMESPACE
          kubectl rollout status deployment/todo-app-dep -n $NAMESPACE
          
          # I. Mostrar servicios
          kubectl get services -o wide